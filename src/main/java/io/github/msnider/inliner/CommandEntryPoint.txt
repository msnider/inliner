package io.github.msnider.inliner;

import java.io.IOException;
import java.net.URISyntaxException;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

@Component
public class CommandEntryPoint implements CommandLineRunner {
	
	@Autowired
	PageProperties pageProps;

	@Autowired
	InlinerService inlinedService;
	
	@Override
	public void run(String...arg0) throws Exception {
		System.out.println("User Agent: " + pageProps.getUa());
		/*try {
			String html = inlinedService.htmlUrl(
					pageProps.getUrl(), 
					pageProps.getContents(), 
					pageProps.getUa(), 
					pageProps.getWidth(), 
					pageProps.getHeight(), 
					pageProps.getDeviceWidth(), 
					pageProps.getDeviceHeight(), 
					pageProps.getDevicePixelRatio(),
					pageProps.getDefaultFontSizePx());
			System.out.print(html);
			System.exit(0);
		} catch (URISyntaxException | IOException e) {
			System.err.println("Error Processing HTML: " + e.getLocalizedMessage());
			System.exit(1);
		}*/
	}
	
}
